version: '3'
services:
  backend:
    build: .
    ports:
      - "8081:8080"
    container_name: seniors-blue
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
networks:
  default:
    driver: bridge